module core;

public type Sequence[+T] extends Collection[T] {
  Sequence[T] replace(T a, T b) {
  }

  Sequence[T] replace(Sequence[T] a, Sequence[T] b) {
  }

  Sequence[T] subsequence(Int a, Int b) {
  }

  # TODO: decide names for "get first/last n elements"
  # front/first/head, back/last/tail

  Bool startsWith(Sequence[T] s) {
  }

  Bool endsWith(Sequence[T] s) {
  }

  public String toString() {
    return "(" + ", ".join(toSequence()) + ")";
  }
}
