module core;

type Char {
  Void init(Int i);

  Char toUpper();
  Char toLower();

  Bool isAlpha();
  Bool isNumeric();
  Bool isAlphaNumeric();

  Bool ==(Object that) {}
  Int hashCode() {}
  String toString() {}
}
