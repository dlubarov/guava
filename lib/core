module core;

type Object {
}

type Int {
  Int +(Int that);
  Int -(Int that);
  Int *(Int that);
  Int /(Int that);
  Int %(Int that);
  Int &(Int that);
  Int |(Int that);
  Int ^(Int that);
  Int <<(Int that);
  Int >>(Int that);
  Int +(Int that);
}
