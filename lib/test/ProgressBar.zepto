module test;

type SimpleTest {
  static Void main() {
    Int w = 70;
    for (Int i = 0; i <= w; i = i + 1) {
      Console.out("\r");
      Console.out("|" * i);
      Console.out("-" * (w - i));
      Console.out(" (" + i + "/" + w + ")");
    }
    Console.out("\n");
  }
}
